# Stage 08 QA

Verify input/output partition is complete and disjoint, original fields are preserved, order is preserved within successful output, generated columns are exact, and manifest values are calculated from re-read artifacts.

