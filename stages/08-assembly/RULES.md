# Stage 08 Rules

Infrastructure may copy validated fields only. It may not construct, rewrite, rotate, or repair email text. Every input row must appear exactly once in either customized or failed output.

