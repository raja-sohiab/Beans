# Stage 10 QA

The release decision must contain final CSV hash, failed CSV hash, QA report hash, KB commit, counts, and explicit blockers. No human-readable success claim may override a failed gate.

