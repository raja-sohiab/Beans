# Stage 07 Rules

`customized` is allowed only when all blocking checks pass. Bounded retry may replace a failed attempt for the same lead only with a new model invocation and full revalidation. No fallback writer.

